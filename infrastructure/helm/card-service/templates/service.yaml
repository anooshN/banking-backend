apiVersion: v1
kind: Service
metadata:
  name: {{ include "card-service.fullname" . }}
  labels:
    {{- include "card-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "card-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
